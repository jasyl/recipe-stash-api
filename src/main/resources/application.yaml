spring.datasource.url=jdbc:postgresql://localhost:5432/recipestashdb
spring.datasource.username=
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.error.include-message=always
logging.level.com.capstone.recipestashapi.recipe=DEBUG

#spring.security.user.password=test

# https://www.toptal.com/spring/spring-security-tutorial
# for some use cases, if you want to put custom filter in front, you need to add padding their order
#spring.security.filter.order=10

api.key=712743876efc4a1ea77de23d3e322a0b